[project]
name = "workers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain[anthropic]>=0.3.27",
    "langgraph>=0.6.4",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
